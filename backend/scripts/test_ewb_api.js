require('dotenv').config();

async function testEWB() {
  const ewaybillno = "211516086691"; // A sample active EWB if possible, or I can just check the mapping logic.
  // Actually, wait, without an ewaybill number, I can't test it.
}
