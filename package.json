{
  "name": "transport-erp-railway",
  "version": "1.0.0",
  "description": "Root package.json for Railway deployment",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
